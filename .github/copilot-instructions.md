## Restricted Files

Do not read, reference, or include content from any `.env` files (except `.env.example`). These files contain secrets and must never appear in suggestions, completions, or chat responses.
